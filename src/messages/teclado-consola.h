#ifndef __teclado_consola_h
#define __teclado_consola_h

/**
 * En este archivo van todas las estructuras de mensajes
 * que se utilicen para comunicar la interfaz con el daemon
 */
typedef struct {
    char str[255];
} line;

#endif
